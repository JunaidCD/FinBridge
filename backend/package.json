{
  "name": "finbridge-backend",
  "version": "1.0.0",
  "description": "Backend for FinBridge DeFi lending platform",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.js --network sepolia",
    "setup": "hardhat run scripts/setup-local.js --network localhost",
    "verify": "hardhat verify"
  },
  "keywords": [
    "defi",
    "lending",
    "ethereum",
    "hardhat"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.0",
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "chai": "^4.5.0",
    "dotenv": "^16.3.1",
    "ethers": "^6.16.0",
    "hardhat": "^2.28.5"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6"
  }
}
